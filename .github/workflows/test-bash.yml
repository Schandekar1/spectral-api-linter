name: bash script job

on:
  push:
    branches: [main]

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run bash script
        run: sudo apt-get install bash
             
        shell: bash
