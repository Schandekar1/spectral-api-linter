name: bash script job

on:
  push:
    branches: [main]

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Run bash script
        run: echo "$PWD"
             
        shell: bash
